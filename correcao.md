#Questao1
Correto
Nota: 1,00

#Questao2
Correto
Nota: 1,00

#Questao3
Correto
Nota: 1,00

#Questao4
Correto
Nota: 1,00

#Questao5
Correto
Nota: 1,00

#Questao6
Correto
Nota: 1,00

#Questao7
Correto
Nota: 1,00

#Questao8
Correto
Nota: 1,00

#Questao9
Correto
Nota: 1,00

#Questao10
Correto
Nota: 1,00

Subtotal: 10,00

Penalizações:
- Colaborador: não verificável.
- Commits: no `shortlog` da `main` **só** aparece **Laboratório FIAP** — não há autor identificável como Daniel, Guilherme ou Pedro; aplica-se **−1,00** pela regra de pelo menos um commit por integrante (sem “complacência” só por histórico bonito).
- `participantes.md`: ok.

Total final: 9,00

🧾 EXPLICAÇÃO DA CORREÇÃO

*Critério:* **10** nas questões em que o propósito do enunciado está cumprido; **micro** diferenças de texto, estilo ou caminho equivalente não descontam. **0,50** quando há erro objetivo ou cobertura incompleta. O código do grupo está muito completo; o desconto é só na participação em commits.*

**Questão 1 — Ambiente:** Comentários trazem saídas equivalentes a `node -v`, `npm -v` e `git --version` (com versões legíveis).

**Questão 2 — HTML + JS inicial:** `console.log("Mensagem inicial")` e `alert("Bem-vindo ao Checkpoint!")`; no HTML, `<h1>` identifica o checkpoint e `script.js` é carregado antes de `</body>`.

**Questão 3 — Repositório e primeiro commit:** Bloco inicial descreve o repositório do checkpoint; o histórico Git mostra sequência de commits alinhados às questões.

**Questão 4 — Variáveis:** Duas `var`, duas `let`, duas `const`, com valores exibidos no console.

**Questão 5 — Aritméticos:** Duas variáveis numéricas (`a`, `b`) e as quatro operações no console.

**Questão 6 — Comparação:** Exemplos com `==`, `===`, `!=`, `!==` e relacionais (`>`, `<`) com `console.log` dos resultados.

**Questão 7 — Lógicos:** Uso de `&&`, `||` e `!` em expressões exibidas no console.

**Questão 8 — Branch e merge:** Comentário alinhado a “Exemplo de branch e merge”; no Git há `origin/feature/questao8` e histórico compatível com integração na `main`.

**Questão 9 — compararNumeros:** Função cobre maior, menor e igual; chamadas de teste coerentes com o enunciado.

**Questão 10 — Operadores no navegador:** Soma com rótulo (`Soma:`) e uso de `&&` / `||` com valores booleanos no console.

---

«CONTEXTO GIT»: `origin` → `D4NIELT0RRES/Checkpoint1_WebDev_1ESPU.git`; `remotes/origin/feature/questao8`; `git shortlog -sn main`: Laboratório FIAP (12).
